// Phase 2: Notification service
// Dispatches push notifications (FCM), emails (SendGrid), and SMS (Twilio)

export class NotificationService {
  // TODO: Implement in Phase 2
  // - sendPush(userId, title, body, data)
  // - sendEmail(to, subject, template, data)
  // - sendSMS(phone, message)
  // - queueNotification(type, payload) — adds to Bull queue
}
