import { Server, Socket } from 'socket.io';

// Phase 3: WebRTC VoIP calling
export function registerCallHandlers(_io: Server, _socket: Socket) {
  // TODO: Implement in Phase 3
  // - call:initiate
  // - call:offer (SDP)
  // - call:answer (SDP)
  // - call:ice (ICE candidate)
  // - call:end
}
